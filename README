- Usage
  -- perl -I../perl test.pl HTML URL 

  -- HTMLで領域に色をつけて表示
     (1) data/style.cssを適当なディレクトリにコピー
     (2) 下記のようにhtmlファイルをstyle.cssと同じディレクトリに掃く
         perl -I../perl test.pl -add_class2html HTML URL > somewhere/test.html
     
To shibuta@nlp.kuee.kyoto-u.ac.jp
